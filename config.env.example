# Example environment variables for the Natours project
# Copy this file to `config.env` and fill in real values before running the app.

# Server
PORT=3000
# NODE_ENV is often set by the start script (e.g. cross-env). You can set it here if you like:
# NODE_ENV=development

# MongoDB connection string
# For local development you can use: mongodb://127.0.0.1:27017/natours
MONGO_URL=mongodb://localhost:27017/natours

# JWT settings
# Use a long, random secret in production
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=90d
JWT_COOKIE_EXPIRES_IN=90

# Email (used for password reset emails)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=your_email_user
EMAIL_PASSWORD=your_email_password

# Notes:
# - Do NOT commit your real `config.env` to source control. Keep secrets out of the repo.
# - On production, use your platform's secret storage or environment configuration.